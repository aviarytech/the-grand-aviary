//This might need changing with the final copy with the actual fields
export interface Location {
    _id: string;
    address: string;
    latitude: number;
    longitude: number;
    description?: string;
    createdAt: string;
    updatedAt: string;
}